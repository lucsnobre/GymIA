{
  "name": "gymia-personalized-workouts",
  "version": "1.0.0",
  "description": "Personalized workout site with AI chat integration",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "echo 'Backend deployment - no build step required'",
    "start": "cd server && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "build:client": "cd client && npm run build",
    "migrate": "cd server && npm run migrate"
  },
  "keywords": ["fitness", "ai", "workout", "personalized", "react", "nodejs"],
  "author": "GymIA Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
